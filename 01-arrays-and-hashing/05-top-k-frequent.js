// 347. Top K Frequent Elements
// Given an integer array nums and an integer k, return the k most frequent elements.
//
// Example 1: nums = [1,1,1,2,2,3], k = 2 -> Output: [1,2]
// Example 2: nums = [1], k = 1 -> Output: [1]
//
// Hint: Count frequencies with a map, then use bucket sort (index = frequency)

function topKFrequent(nums, k) {
  // Your solution here
}

// Tests
console.log(topKFrequent([1, 1, 1, 2, 2, 3], 2)); // Expected: [1, 2]
console.log(topKFrequent([1], 1)); // Expected: [1]
